<h1>Burgers</h1>

<h2>Burgers to be DEVOURED</h2> 

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>
        {{id}}.{{burger_name}}
        <button class='changeDevour' data-id="{{id}}" data-newdevour="{{devoured}}">Devour Me!!</button>
      </li>
    {{/unless}}
  {{/each}}

</ul>

<h2>Burgers already DEVOURED</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
    <li>
      {{id}}.{{this.burger_name}}</p>
    </li>
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgers">Type of burger</label>
    <input type="text" id="burgers" name="burger_name">
  </div>

<button type="submit">Eat Da Buger</button>