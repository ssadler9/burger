<h1>Burgers</h1>

<h2>Burgers to be DEVOURED</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
      <li>
        <p><span>Burger:</span> {{this.burger_name}}</p>
      </li>
          {{> burgers-block devoured=false}}
    {{/unless}}
  {{/each}}

</ul>

<h2>Burgers already DEVOURED</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
    <li>
      <p><span>Burger:</span> {{this.burger_name}}</p>
    </li>
        {{> burgers-block devoured=true}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="burgers">Type of burger</label>
    <input type="text" id="burgers" name="burger_name">
  </div>

<button type="submit">Eat Da Buger</button>